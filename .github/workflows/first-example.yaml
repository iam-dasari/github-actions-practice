name: My First Workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "My First GitHub Actions Job"

      - name: Checkout the code
        uses: actions/checkout@v3
        
      - name: List Files
        run: ls
        
      - name: Read file
        run: cat workflow1.yaml
